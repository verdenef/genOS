I have downloaded a newer version of the GameOS framework and placed it in a temporary folder named `_GameOS_Update/` in the root of my repository.

Your task is to synchronize this project's active `GameOS/` folder with the latest `_GameOS_Update/` framework.

Requirements:

1. Compare the current project's `GameOS/` directory against `_GameOS_Update/`.
2. Identify:
   - New files
   - Modified files
   - Removed files
   - Potential conflicts
3. Preserve all project-specific data (`00_PROJECT.md`, `30_DECISIONS.md`, `50_ROADMAP.md`, `20_PROGRESS.yaml`, `systems/*.md`, `60_AGENTS.md`, etc.).
4. Apply only framework improvements (such as kernel updates in `AGENTS.md` or new guides/templates).
5. Never overwrite project knowledge with template content.
6. Explain every migration that should occur.
7. Wait for my approval before editing any files.

Treat this as a framework upgrade, not a fresh installation.
